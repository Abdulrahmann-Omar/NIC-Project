---
description: Auto-approve all commands including git push and editing files and pip installs
---

// turbo-all

This workflow enables auto-running all commands without user approval.

When this workflow is active:
- File changes are auto-approved
- Git push/commit auto-approved
- All terminal commands auto-run